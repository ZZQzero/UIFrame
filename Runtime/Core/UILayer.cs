namespace UIFrame
{
    /// <summary>UI 层。排序由 <see cref="UIFrameRoot"/> 按兄弟顺序叠放。</summary>
    public enum UILayer
    {
        Window = 0,
        Hud,
        Mask,
        Popup,
        Tips,
        Guide,
    }
}
